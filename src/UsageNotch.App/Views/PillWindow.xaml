<Window x:Class="UsageNotch.App.Views.PillWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:UsageNotch.App.Controls"
        xmlns:conv="clr-namespace:UsageNotch.App.Converters"
        Title="UsageNotch — pilule"
        WindowStyle="None" AllowsTransparency="True" Background="{x:Null}"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False" ResizeMode="NoResize"
        Width="64" Height="136" UseLayoutRounding="True" SnapsToDevicePixels="True">
  <Window.Resources>
    <conv:HexBrushConverter x:Key="Hex" />
    <conv:ActivityVisibilityConverter x:Key="Activity" />
    <BooleanToVisibilityConverter x:Key="BoolVis" />
    <!-- Cadence plafonnée : chaque image d'une fenêtre transparente est rendue en logiciel. Un fondu d'opacité reste fluide
         à 20 images/s ; la rotation garde 30 images/s pour ne pas saccader. Le repli (200 ms) reste à 60 images/s. -->
    <Storyboard x:Key="Spin" RepeatBehavior="Forever" Timeline.DesiredFrameRate="30">
      <DoubleAnimation Storyboard.TargetName="SpinRotate" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:1.2" />
    </Storyboard>
    <Storyboard x:Key="Pulse" RepeatBehavior="Forever" AutoReverse="True" Timeline.DesiredFrameRate="20">
      <DoubleAnimation Storyboard.TargetName="LogoTint" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:1" />
    </Storyboard>
    <Storyboard x:Key="BandPulse" RepeatBehavior="Forever" AutoReverse="True" Timeline.DesiredFrameRate="20">
      <DoubleAnimation Storyboard.TargetName="BandPath" Storyboard.TargetProperty="Opacity" From="0.4" To="1" Duration="0:0:1" />
    </Storyboard>
  </Window.Resources>

  <Window.ContextMenu>
    <ContextMenu>
      <MenuItem Header="Rafraîchir maintenant" Command="{Binding RefreshCommand}" />
      <MenuItem Header="Garder la carte ouverte" IsChecked="{Binding Locked, Mode=OneWay}" Command="{Binding ToggleLockCommand}" />
      <Separator />
      <MenuItem Header="Réglages…" Click="OnSettingsClick" />
      <MenuItem Header="Quitter" Click="OnQuitClick" />
    </ContextMenu>
  </Window.ContextMenu>

  <Canvas x:Name="Root">
    <Path x:Name="BandPath" Visibility="Collapsed"
          Fill="{Binding Cell.BandColor, Converter={StaticResource Hex}}" />

    <Canvas x:Name="PillLayer">
      <Canvas.RenderTransform>
        <TranslateTransform x:Name="Slide" />
      </Canvas.RenderTransform>

      <Path x:Name="PillPath" StrokeThickness="1"
            Fill="{Binding Theme.PillBackground, Converter={StaticResource Hex}}"
            Stroke="{Binding Theme.PillBorder, Converter={StaticResource Hex}}"
            Opacity="{Binding Theme.PillOpacity}" />

      <Grid x:Name="Body">
        <StackPanel x:Name="BodyStack" HorizontalAlignment="Center" VerticalAlignment="Center">
          <StackPanel.LayoutTransform>
            <ScaleTransform x:Name="BodyScale" ScaleX="1" ScaleY="1" />
          </StackPanel.LayoutTransform>

          <Grid x:Name="RingHost" Width="56" Height="56" Margin="0,0,0,4">
            <controls:ProgressRing Width="56" Height="56" RingThickness="4"
                                   TargetFraction="{Binding Cell.Rings[0].Fraction}"
                                   RingBrush="{Binding Cell.Rings[0].Color, Converter={StaticResource Hex}}"
                                   TrackBrush="{Binding Cell.Rings[0].TrackColor, Converter={StaticResource Hex}}" />
            <controls:ProgressRing Width="44" Height="44" RingThickness="4"
                                   TargetFraction="{Binding Cell.Rings[1].Fraction}"
                                   RingBrush="{Binding Cell.Rings[1].Color, Converter={StaticResource Hex}}"
                                   TrackBrush="{Binding Cell.Rings[1].TrackColor, Converter={StaticResource Hex}}" />
            <controls:ProgressRing Width="32" Height="32" RingThickness="4"
                                   TargetFraction="{Binding Cell.Rings[2].Fraction}"
                                   RingBrush="{Binding Cell.Rings[2].Color, Converter={StaticResource Hex}}"
                                   TrackBrush="{Binding Cell.Rings[2].TrackColor, Converter={StaticResource Hex}}" />
            <Grid x:Name="LogoHost" Width="20" Height="20" RenderTransformOrigin="0.5,0.5">
              <Grid.RenderTransform>
                <RotateTransform x:Name="SpinRotate" />
              </Grid.RenderTransform>
              <Path Data="{x:Static controls:BrandGeometry.Mark}" Stretch="Uniform"
                    Fill="{Binding Cell.ActivityMutedColor, Converter={StaticResource Hex}}" />
              <Path x:Name="LogoTint" Data="{x:Static controls:BrandGeometry.Mark}" Stretch="Uniform"
                    Fill="{Binding Cell.ActivityColor, Converter={StaticResource Hex}}" />
            </Grid>
          </Grid>

          <TextBlock x:Name="PercentText" FontFamily="Segoe UI" FontSize="14" FontWeight="SemiBold"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Text="{Binding Cell.PercentText}"
                     Foreground="{Binding Cell.TextColor, Converter={StaticResource Hex}}"
                     Visibility="{Binding Cell.ShowPercent, Converter={StaticResource BoolVis}}" />
        </StackPanel>
      </Grid>
    </Canvas>
  </Canvas>
</Window>
